select
2^3 as squared_amount, --This is the squared amount
abs(-10), --This gives you the value without '-' 
round(12.47), -- this basically rounds '.' value to the nearest number
round(12.6789,2),--round to decimal places
ceiling(4.3), --it always rounds up to the next number no matter if the decimal is small
floor(4.3) --it always rounds down number before to it









